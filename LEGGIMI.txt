PATCH MIRATA PREZZI AXPO / E.CO — NESSUNA CANCELLAZIONE, NESSUNA NUOVA API

Branch previsto:
fix/prezzi-axpo-eco-no-deletions

Cosa corregge:
1. E.CO Energia Corrente viene riconosciuta solo con riferimenti precisi:
   - E.CO con il punto
   - Energia Corrente
   - codice offerta che inizia con 000742
   La parola generica "Eco" non basta più.

2. Nelle offerte variabili, un valore etichettato come spread viene classificato
   prima dei pattern generici di prezzo. Il prezzo confrontato diventa indice + spread.

Cosa NON fa:
- non elimina file;
- non modifica public/index.html;
- non cambia liste partner, link affiliati o monetizzazione;
- non crea endpoint o API;
- non rigenera automaticamente i cataloghi;
- non cambia la struttura del calcolatore.

COME APPLICARLA

A) Con una copia locale del repository
1. Estrai questa cartella.
2. Apri Terminale/Prompt nella cartella estratta.
3. Esegui:

   python APPLICA_PATCH.py /percorso/del/repository

   Su Windows può essere:
   py APPLICA_PATCH.py C:\percorso\offertalogica-vercel

4. Verifica:

   python VERIFICA_PATCH.py /percorso/del/repository

5. Carica sul branch SOLO questo file modificato:

   scripts/update-arera-menu.py

Non caricare nel repository APPLICA_PATCH.py, VERIFICA_PATCH.py o il file .patch.

SICUREZZA
- Lo script controlla lo SHA Git del file del branch:
  5d447da243433f78802436bae1984b184564d78b
- Se la base è diversa, non modifica nulla.
- Prima della modifica crea:
  scripts/update-arera-menu.py.bak-prezzi-axpo-eco
- Se la compilazione Python fallisce, ripristina automaticamente il backup.

CONTROLLO DEL DIFF
Dopo l'applicazione devono risultare modificati soltanto due blocchi nel file
scripts/update-arera-menu.py. Non devono comparire file eliminati né file sotto api/.
